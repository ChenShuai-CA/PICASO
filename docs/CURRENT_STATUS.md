# 当前实施状态（2026-09-19）

## 2026-09-19：P3.3.4 运动学修复 R2-E 验收闭环（当前主线）

第三方审计（`runs/20260918_p334_r2_codex_audit/`）确认 C-v3 核心修复与主指标后，本轮补齐五项验收缺口并闭环（全部产物在 `runs/20260918_p334_r2_closure/`，报告 `R2_CLOSURE_REPORT.md`，清单 `ARTIFACT_MANIFEST_V2.json` 64 文件零缺失）：

- **交叉核对**：合并推理 pass 与审计独立复算逐值相等（C-v3 场景 0/2/2、C-v1 294/7,956/7,956、group_level/diversity/validity/type_checks、11,586 样本 SHA）；vendored 检查与正式 v1 评估器在 1,802,412 候选上零分歧。
- **场景双口径**（用户裁决悬置，`EVAL_PROTOCOL_V3.1_DRAFT.md`）：全 present 口径 (a)/(b)/(c) = 0/13/13 vs 主目标 0/2/2；skip 账目 154,074 = 100,134 + 53,520 + 420。
- **数值账本**：fp32 输出的 26 accel + 2,351 jerk 类型界超限逐候选落盘，**100% 归因 output_quantization**；fp64 重积分 0/0。
- **评估器**：正式 `full_anchored_validity` 落地 validity_version=2（双端传播 + 非有限锚显式失败，用户裁决），缺省 1 逐字节保封存重放；7 项直测单测；真实 dev 三参考 600,804×3 候选 v1→v2 翻转 = 0（审计反例为合成边界）。
- **fp64 输出修订臂**（用户裁决）：C-v3-fp64out 同权重不重训，类型界 0/0、minADE 差 ~1e-8 m、场景 0/2/2、速度失败 12/2/276 保持。
- **五臂延迟**（真实 B 负载）：orig+B 端到端 56.98 vs C-v3 41.07 ms/样本 → **C-v3 快 1.39×（有实测）**；C-v3 生成 40.47 与封存 41.47 口径一致；运动学头 +1.93 ms（+5%）。

**措辞约束（论文与后续报告一律遵守）**：INTERACTION 分源 minADE +0.43%，只称"macro 与冻结误差门槛通过、多数指标改善"，不称全面优于 orig；diversity 96.48% 只称三个 C 后继臂中最高；C-v2 1.3002 为约束失效下真实读数；penalty 暂缓且硬约束 vs 软罚项比较问题未解决；快车归因纠正为 v0 越限+学习加速度（恒速反例可行）；延迟口径 1.39× 非 1.83×。

**下一步**（待用户）：① 场景口径裁决（V3.1 草案 Option A/B）；② P3.3.5 500-shard 准备的预算审核（数据就绪/固定 dev/短探针后停）。全套测试 253 通过；heldout 全程未读；runs/ 产物不入 git（manifest 哈希管理）。

---

## 2026-09-11 状态（历史记录）

## 工作方式

唯一工作区：`D:\Projects\Scenario_Generation_Research`，Ubuntu 路径 `/mnt/d/Projects/Scenario_Generation_Research`。开发、测试、数据处理和训练均由 WSL2 `Ubuntu` 执行。解释器 `/home/shuai/.venvs/scenario-gpu/bin/python`。

## 本轮已完成

- 主代码路径与依赖审查，旧文档提取路径修正，统一 Ubuntu 启动脚本。
- 40 项测试通过，包括真实 CUDA 训练、单双目标掩码、遮挡、回放、数据分组与配额检查。
- 联合公共样本 6,601 个，涉及 131 个来源组；训练 5,331，验证 1,122，测试 148。验证集不能用于训练。
- 完成 GPU 预训练、三个混合训练种子、独立单/双目标训练、无先验和无稳健性后训练消融。
- 25 个预实验任务全部完成，包含共同初始化评价、另一主车控制器、敏感性扰动、单案例 CEM、实时性和回放。
- 单/双目标各 10,000 个决策步：p99 约 3.00 / 4.92 ms，无超过 100 ms 的决策。无渲染；不是硬实时认证。
- 24 条 ABD 记录的通道与控制来源待核对表已生成；未生成未经证实的 AEB 或碰撞标签。

## 结果判断

这轮结果只确认端到端工程链路可运行，未证明统一方法优于强基线。单目标危险率为 15%–25% 这一小范围，双目标结果存在明显种子波动，每分支仅 20 个测试初始条件，不支持显著性或发表水平判断。

脚本双目标对照的 20 次尝试有 9 次目标间碰撞而无效；混合 seed17 有 10 次目标间碰撞、7 次遮挡车角色违规。不能通过删除这些尝试抬高成功率。

## 接下来的实施顺序

1. 改进双目标初始化与有效性诊断：区分参考脚本已冲突、策略诱发目标间冲突、角色违规；保留分层统计和固定测试集，不事后筛掉失败样本。
2. 将自身运动先验升级为带对齐邻居和道路上下文的交互先验，扩大明确行人样本；增加分角色验证和零动作基线。
3. 统一 RL 与参数/轨迹搜索的交互预算，在多个初始条件上进行搜索比较，补齐独立自然性指标和目标角色消融。
4. 核对 ABD 真实控制来源与事件区间，再建立可追溯的响应/执行误差参数；在此之前仍标注为假设扰动。
5. 通过上述检查后再扩大训练和正式测试规模，组织英文论文方法和实验章节。

## 证据入口

- [运行说明](../README.md)
- [预实验报告](../runs/20260911_gpu_pilot/REPORT.md)
- [全部任务状态与命令](../runs/20260911_gpu_pilot/jobs.json)
- [Linux 环境检查](../runs/20260911_compatibility/environment.json)
- [ABD 人工核对表](../runs/20260911_abd_review/manual_review.csv)
- [联合数据清单](../runs/20260911_public_v4/report.json)

Waymo 读取字段已对照官方 Scenario proto 核查：https://raw.githubusercontent.com/waymo-research/waymo-open-dataset/master/src/waymo_open_dataset/protos/scenario.proto 。当前未解码地图与传感器字段，不宣称完成全场景预训练。

Git 中原有 Stage3 文档的 CRLF/尾随空白差异及旧文件删除记录保持不变；它们不是本轮新代码检查失败。旧采样目录保留为排错记录，以 README 指定的 v4 数据为准。
