{
  "phase_c_spec": "runs/20260918_p334_r2_closure/R2_CLOSURE_REPORT.md (R2-E output-precision revision arm, user adjudication 2026-09-19); identical flags to model_kinematic_c_v3.json plus output_position_precision -- v3 and earlier configs stay sealed and replay bit-exact",
  "construction": "bounded",
  "token_conditioning": true,
  "start_pair_joint": true,
  "start_recurrence_version": 2,
  "output_position_precision": "float64",
  "v0_source": "recorded",
  "v0_speed_limit_mps": 35.0,
  "limits_by_type": {
    "0": [19.0, 9.5],
    "1": [19.0, 9.5],
    "2": [5.0, 3.0],
    "3": [10.0, 5.0],
    "4": [10.0, 5.0]
  },
  "start_pair_cap_mps2": 9.5,
  "loss": {
    "token_intent_huber": 0.1,
    "speed_exceedance": 0.05,
    "jerk_exceedance": 0.5,
    "accel_exceedance": 0.5
  },
  "r2_change_log": {
    "start_recurrence_version": "step 0 of the joint start pair now advances the working acceleration to a_1_raw, so step 1 computes a_2 = disk(a_1_raw + h*j_2) (true continuous recurrence) instead of disk(a_0 + h*j_2); the step-1 joint rescale applies one scale to both terms, capping the realized start jerk at J instead of 2J (38 -> <=19 m/s^3 for vehicles; R1_ERRATA 2.2, SPEC_R2 2.2)"
  },
  "r2e_change_log": {
    "output_position_precision": "C-v3-fp64out arm: same weights as C-v3 (runs/20260917_p334_r2/c_v3_train/arch_best_checkpoint.pt), no retraining; positions re-integrate the same v_seq with a float64 anchor and float64 cumsum. Removes the fp32 output quantization residual behind the 26 accel / 2351 jerk type-bound exceedances (FULL_DEV_REEVAL.json type_checks: fp64 reintegration -> 0/0). Evaluation reports fp32 and fp64 output arms in separate tables; the frozen C-v3 (fp32) readings are preserved unchanged."
  },
  "control_variant": {
    "construction": "penalty",
    "note": "unchanged v1 policy: deferred; no hard-vs-soft ablation claim is made (R2-E wording corrections)"
  }
}
